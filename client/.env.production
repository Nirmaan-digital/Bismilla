# Used automatically by `npm run build` (Vite defaults to production mode).
# Points the built app at your live backend instead of localhost.
VITE_API_URL=https://backend.bismillahchickencenter.com/api