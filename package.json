{
  "name": "bismilla",
  "version": "1.0.0",
  "private": true,
  "description": "Bismilla Chicken Center - deploy wrapper. The real apps live in server/ and client/.",
  "scripts": {
    "build": "cd server && npm install --omit=dev",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}